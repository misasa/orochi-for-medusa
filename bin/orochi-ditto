#!/usr/bin/env ruby
require 'orochi_for_medusa'

OrochiForMedusa::Runner.new.run(ARGV, :command_name => File.basename($0, '.*'))

#require 'orochi_for_medusa/runner'

#OrochiForMedusa::Runner.run(ARGV, :program_name => File.basename($0, '.*'))
#cui = OrochiForMedusa::Cd.new(ARGV, :program_name => File.basename($0, '.*'))
#cui.run 

